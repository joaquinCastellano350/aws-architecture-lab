# Contracts

OpenAPI will be the source of truth for the public HTTP API. JSON Schema will be the source of truth for commands, outcomes, and domain events. TypeScript types will be generated rather than maintained independently.
