{
  "name": "@aws-architecture-lab/contracts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "generate": "node scripts/generate-checkout-api.mjs && node scripts/generate-order-command.mjs && node scripts/generate-order-event.mjs",
    "build": "npm run generate && tsc -b",
    "clean": "tsc -b --clean",
    "test": "npm run generate && vitest run --passWithNoTests",
    "typecheck": "npm run generate && tsc -b --pretty false"
  }
}
